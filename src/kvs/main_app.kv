# 侧边栏模窗
<SidebarModalView>:
    id: sidebar_view
    overlay_color: app.overlay_color
    size_hint: 0.15, 1
    pos_hint: {'x':0}
    PartScrollView:
        PartBoxLayout:
            size_hint: 1,None
            orientation: 'vertical'
            spacing: dp(8)
            padding: dp(8)
            height: self.minimum_height

            BottomLineLabel:
                id: page_skinManage
                text: 'Skin Manage'

# 主程序布局
<MainLayout>:
    TestBoxLayout:
        id: main_content_layout
