# 只有图标的Label按钮
<ClickIconLabel>:
    size_hint_x: None
    width: dp(30)
    Image:
        source: root.icon_source
        pos: root.x + dp(5), root.y + dp(5)
        size: root.width-dp(10), root.height-dp(10)